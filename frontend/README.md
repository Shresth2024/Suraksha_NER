# SURAKSHA NER — Frontend

This frontend is connected to the Django backend included in the parent `backend` folder.

Run the backend at `http://127.0.0.1:8000`, then open `index.html` through VS Code Live Server.

The JavaScript calls the backend API for registration, login, location, risk zones, alerts and admin data.
